name: VS Code Extension

on:
  push:
    branches: [main]
    paths:
      - 'vscode-extension/**'
      - '.github/workflows/vscode-extension.yml'
  pull_request:
    paths:
      - 'vscode-extension/**'
      - '.github/workflows/vscode-extension.yml'

defaults:
  run:
    working-directory: vscode-extension

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 20
      - run: npm ci
      - run: npm run build
      - name: Unit tests
        run: npm run test:unit
      # Provision a VS Code-compatible editor (VSCodium) from GitHub so the
      # integration tests never depend on Microsoft download hosts — this keeps
      # them runnable under egress allowlists that block update.code.visualstudio.com.
      - name: Provision test editor
        run: npm run setup:test-editor
      - name: Integration tests (Extension Host)
        run: xvfb-run -a npm run test:integration
